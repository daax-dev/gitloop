{"id":"arch-001","date":"2026-05-27","decision":"gitloop is a pure-TypeScript reframe of issue #1's Python design: Node 22 LTS (pinned via .nvmrc), pnpm, Vitest, tsx, strict tsc; notifier and the three MCP tools in TS; the post-push git hook is a thin shell stub with no pipeline logic. Repo is a standalone git repo, remote daax-dev/gitloop.","rationale":"Operator directive: pure-TypeScript tool. git requires an executable hook script, the only non-TS artifact. Standalone repo matches peer src/* subprojects (each its own repo; dx ignores src/).","alternatives":"Keep the issue's Python Flask notifier + Python MCP (rejected: operator wants TS); hook as a Node shebang script (deferred: shell stub execing Node is simpler/portable); keep gitloop inside the dx repo (rejected: peers are separate repos).","references":["https://github.com/daax-dev/gitloop/issues/1"]}
{"id":"arch-002","date":"2026-05-27","decision":"Tag-push events reach the tag-notifier via git's pre-push client hook (fire-and-forget HTTP POST), not a post-push hook. OBJECTIVE.md/PRD reference to .git/hooks/post-push is corrected: git has no client-side post-push hook.","rationale":"Operator decision. githooks(5) defines pre-push as the only push-time client hook; payload (local ref, local sha, remote ref, remote sha) arrives on stdin. Fire-and-forget keeps git push native and avoids blocking on the notifier.","alternatives":"post-receive on a shared bare repo (rejected: adds bare-repo topology setup); explicit gitloop push wrapper (rejected: loses native git push); notifier polling for-each-ref (rejected: least event-driven). Accepted risk: notify can fire before a push that subsequently fails.","references":["https://git-scm.com/docs/githooks#_pre_push"]}
{"id":"arch-003","date":"2026-05-27","decision":"No CI/test gate on the test-complete transition. Validation is worker-driven: the receiving worker performs testing/review; on failure it retags the task backward with a version increment (rejection loop-back). advance_task does not enforce a passing test run.","rationale":"Operator decision; resolves OBJECTIVE open question 1. CI is explicitly out of scope. Determinism is preserved because the loop-back is itself a tag transition verifiable from git state.","alternatives":"Require local test evidence artifact before test-complete (rejected: re-introduces a gate where the next worker's validation already covers correctness).","references":["https://github.com/daax-dev/gitloop/issues/1"]}
{"id":"arch-004","date":"2026-05-27","decision":"The pipeline is config-driven: ordered steps are defined in a gitloop pipeline config, not hardcoded to the six OBJECTIVE steps. Steps can be added/renamed. Each step may optionally require a per-step completion markdown artifact (e.g. review.md) committed to git before that step's tag is legal. Resolves OBJECTIVE open question 3, generalized beyond review.md.","rationale":"Operator decision. Generalizing the review.md gate to any step keeps advance_task's artifact-requirement logic uniform and lets operators tailor the pipeline. The six OBJECTIVE steps ship as the default config.","alternatives":"Hardcode six steps with a review.md-only gate (rejected: operator wants add/rename steps); no artifact gate at all (rejected: operator wants per-step gate option).","references":["https://github.com/daax-dev/gitloop/issues/1"]}
{"id":"arch-005","date":"2026-05-27","decision":"Worker claim atomicity is enforced by pushing a claim tag to the shared remote; the remote ref update is atomic and rejects a duplicate, so the losing worker fails cleanly. No filesystem lock or state outside git.","rationale":"Operator decision; resolves the claim-race design. git ref updates on push are atomic per-ref, giving a compare-and-swap primitive without an external lock, preserving the no-state-outside-git invariant.","alternatives":"Local lockfile/advisory lock (rejected: introduces state outside git, violates the core invariant).","references":["https://git-scm.com/docs/git-push"]}
{"id":"arch-006","date":"2026-05-27","decision":"Tag grammar and determinism rules: (a) the entry/registration state is the bare tag TASK-<id> (the only pipeline tag without a slash); git refs cannot begin with '/', so OBJECTIVE's /TASK-N is notation, not a literal tag. (b) Step tags are <step>/TASK-<id> with an optional /v<K> version suffix; v1 is implicit (no suffix), versions >1 are explicit. (c) A task's current version is the max /vK present across its tags; after a rejection loop-back every forward step is re-pushed carrying that version (e.g. {in-progress/TASK-9, code-complete/TASK-9, in-progress/TASK-9/v2} -> next legal action is advance to code-complete/TASK-9/v2, not 'already complete'). (d) Per-step required-artifact paths are templates with {taskId} substitution (e.g. tasks/{taskId}/review.md), checked for existence against the commit the current step's tag points at.","rationale":"Encodes the determinism guarantee unambiguously and avoids tag-name collisions on re-advance. Bare entry tag sidesteps git ref-format rules. Max-version derivation makes loop-back state reconstructable from the tag set alone. Templated artifact paths enable per-task gates; checking against the step tag's commit ties the gate to the work being advanced.","alternatives":"Namespaced entry tag task/TASK-N (rejected: couples parsing to a reserved word; bare tag is unambiguously the no-slash case). Explicit /v1 on every tag (rejected: noisier, diverges from OBJECTIVE notation). Global (non-templated) artifact path (rejected: prevents per-task artifacts). Checking artifact at HEAD (rejected: not tied to the advanced commit).","references":["https://git-scm.com/docs/git-check-ref-format"]}
{"id":"arch-007","date":"2026-05-27","decision":"A worker claim is an annotated git tag claim/<step>/TASK-N[/vK] whose ref name carries no worker identity; the claimer id is written in the tag's (single-line) message. The anonymous ref name makes the push an atomic compare-and-swap (first worker to push wins; the loser's push is rejected as already-exists), while the owner reads back the tag message to make re-claiming its own step an idempotent no-op and others can see who holds the claim. Worker identity comes from the GITLOOP_WORKER_ID environment variable (falling back to user@host), not from a new MCP tool parameter, preserving the OBJECTIVE's three tool signatures.","rationale":"Operator decision. Putting identity in the ref name would give each worker a distinct ref, defeating the atomic CAS; putting it in the tag message keeps CAS on the ref while still recording identity in git (no state outside git). Env-var identity keeps get_pending_tasks/claim_task/advance_task signatures as specified in OBJECTIVE.","alternatives":"Worker id in the ref name (rejected: distinct refs lose CAS arbitration, forcing the advance tag to be the real winner). No claim tag, advance-is-claim (rejected: loses the in-progress/taken signal; two workers can duplicate work before one push is rejected). workerId as an explicit tool parameter (rejected: extends the OBJECTIVE tool signatures; env var suffices).","references":["https://git-scm.com/docs/git-push"]}
{"id":"arch-008","date":"2026-05-27","decision":"Supersedes arch-006b: pipeline step-tag versions are ALWAYS explicit — v1 is /v1, not implicit. A step tag is always <step>/TASK-N/vK (k>=1); the entry tag remains bare TASK-N. Reason: the implicit-v1 scheme is impossible in git — refs/tags/<step>/TASK-N (a ref/file) and refs/tags/<step>/TASK-N/v2 (which needs <step>/TASK-N to be a directory) are a git directory/file conflict ('cannot lock ref ... exists'). Making <step>/TASK-N always a directory (every version a child vK) removes the conflict. Claim tags follow: claim/<step>/TASK-N/vK.","rationale":"Discovered empirically: git tag in-progress/TASK-2/v2 fails with 'fatal: cannot lock ref refs/tags/in-progress/TASK-2/v2: refs/tags/in-progress/TASK-2 exists'. Always-explicit versions keep the parent a directory and the grammar uniform (step tags always parse to 3 segments), which also simplifies derivation. OBJECTIVE's <step>/TASK-N notation was illustrative, not a literal ref (it also showed the git-illegal leading-slash /TASK-N).","alternatives":"Suffix the version on the task segment as <step>/TASK-N.vK keeping v1 unsuffixed (rejected: mixes '/' hierarchy with '.' suffix, less uniform parsing, though closer to OBJECTIVE notation). Put version before task as <step>/vK/TASK-N (rejected: less readable). Keep implicit v1 (rejected: impossible in git per the D/F conflict).","references":["https://git-scm.com/docs/git-check-ref-format"]}
{"id":"arch-009","date":"2026-05-27","decision":"Pipeline config is loaded at process startup in this order: GITLOOP_CONFIG=<path> (explicit JSON file, error if unreadable/invalid) → <repoPath>/.gitloop/pipeline.json (auto-discovered if present) → DEFAULT_PIPELINE. File IO lives in core/config-file.ts; validation stays in the pure core/config.ts. Wired into depsFromEnv (MCP server) and the notifier entry point.","rationale":"Operator decision: JSON file plus .gitloop/ auto-discovery. Without a file loader, a running MCP server/notifier could only ever use the six default steps, so custom pipelines (e.g. adding a plan/design step) were undocumentable/unusable at runtime. JSON reuses the existing validator with zero new dependencies.","alternatives":"GITLOOP_CONFIG JSON only, no discovery (rejected: less ergonomic). YAML support (deferred: adds a parser dependency to vet). Embedding config in code only (rejected: the whole point is config-driven pipelines at runtime).","references":["https://github.com/daax-dev/gitloop/issues/1"]}
{"id":"arch-010","date":"2026-05-27","decision":"The notifier event is an ADVISORY wake-up signal, not the source of truth. Because the hook is pre-push (arch-002), an event can reach the notifier microseconds before the tag actually lands on the remote (or for a push that is subsequently rejected). Workers must always re-derive authoritative state from git tags (fetchTags + listTags) — which the MCP tools already do — and never act on the event payload alone. The hook is best-effort: if the notifier is down or curl is missing, the push still succeeds (verified in the sandbox: connection-refused returns in ~0.2s, exit 0; a blackholed host is bounded by curl -m 2). This is documented in troubleshooting/FAQ, not 'fixed'.","rationale":"Validated empirically via scripts/sandbox.ts: a full /TASK-1 → merged run with a rejection fired the hook on every push and the notifier received all events, while a notifier-down push still succeeded fast. git's atomicity, not event ordering, is what guarantees correctness, so advisory-event semantics are sufficient and keep the loop simple (resolves OBJECTIVE open question 2: polling suffices locally; no SSE needed).","alternatives":"Make the event authoritative / block the push until acknowledged (rejected: couples push success to notifier availability, breaks graceful degradation). Switch to post-receive on a bare remote for after-push timing (rejected earlier in arch-002; would change topology).","references":["https://git-scm.com/docs/githooks#_pre_push"]}
